const jobId=new URLSearchParams(location.search).get("job_id"),form=document.querySelector("#review-form"),note=document.querySelector("#review-note");
async function init(){if(!jobId){form.hidden=true;note.textContent="Auftrags-ID fehlt.";return;}const r=await fetch(`/api/reviews/eligibility?job_id=${encodeURIComponent(jobId)}`),d=await r.json();if(!r.ok||!d.eligible||d.submitted){form.hidden=true;note.textContent=d.submitted?"Für diese Konvertierung wurde bereits eine Bewertung abgegeben.":(d.error||"Bitte öffnen Sie diese Seite aus dem angemeldeten Kundenkonto.");}}init();
form.addEventListener("submit",async e=>{e.preventDefault();const data=new FormData(form),r=await fetch("/api/reviews",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({job_id:jobId,rating:Number(data.get("rating")),text:data.get("text")})});if(r.ok){form.replaceChildren(Object.assign(document.createElement("p"),{textContent:"Vielen Dank. Ihre Bewertung wurde gespeichert."}));}else{note.textContent=(await r.json()).error;}});
